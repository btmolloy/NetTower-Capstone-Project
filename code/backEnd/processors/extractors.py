code/backEnd/processors/extractors.py

Purpose: Normalize sensor outputs into canonical Event objects (especially if sensors emit raw lines).
Inputs: Raw sensor event forms or raw strings/records.
Outputs: Canonical Event objects from models/events.py.