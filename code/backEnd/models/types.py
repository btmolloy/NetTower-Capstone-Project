Purpose: Shared enums and small shared types used across events/entities to prevent string inconsistency.
Inputs: None (definitions only).
Outputs: Types like:

SensorSource (TCPDUMP, NMAP, PING)

Protocol (TCP, UDP, ICMP, ARP)

Confidence levels, etc.