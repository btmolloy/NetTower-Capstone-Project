#Purpose: Minimal frontend server (temporary) to load UI pages and later call backend API.
#Inputs: User browser requests; later calls to backend API endpoints.
#Outputs: Rendered HTML templates and static assets; later displays topology data.